// @flow 
import * as React from 'react';
// import { useFormStatus } from "react-dom";
type Props = {
    
};
const DemoUseFormStatus = (props: Props) => {
    // const status = useFormStatus();

    return <>
        - thằng này dùng như formik thôi
        <br />
        - cài react dom vào mà dùng
    </>
};

export default DemoUseFormStatus;